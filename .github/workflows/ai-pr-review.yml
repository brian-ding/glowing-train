name: AI PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened]
  # workflow_dispatch: {}

jobs:
  ai-pr-review:
    runs-on: hackthon
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: test url
        run: echo "${{ github.event.pull_request.url }}"

      # - name: PR summary
      #   run: python summary -p ${{ github.event.pull_request.url }}

      # - name: PR code review
      #   run: python review -p ${{ github.event.pull_request.url }}